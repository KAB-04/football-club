'use client'
import{useFormStatus}from'react-dom';import{setMessageStatusAction}from'@/app/admin/(protected)/messages/actions';import{Button}from'@/components/ui/button';function Submit({status}:{status:'read'|'resolved'}){const{pending}=useFormStatus(),label=status==='resolved'?'Mark as Resolved':'Mark as Read';return <Button disabled={pending} type="submit" variant={status==='resolved'?'primary':'outline'}>{pending?(status==='resolved'?'Resolving...':'Marking...'):label}</Button>}export function MessageStatusForm({id,status}:{id:string;status:'read'|'resolved'}){return <form action={setMessageStatusAction}><input name="id" type="hidden" value={id}/><input name="status" type="hidden" value={status}/><Submit status={status}/></form>}
