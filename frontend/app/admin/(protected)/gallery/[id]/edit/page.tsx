import{notFound,redirect}from'next/navigation';import{AdminPageHeader}from'@/components/admin/admin-page-header';import{GalleryItemForm}from'@/components/admin/gallery-item-form';import{getCurrentAdmin}from'@/lib/auth/admin';import{getAdminGalleryById}from'@/lib/data/admin/gallery';export default async function Page({params}:{params:Promise<{id:string}>}){if(!await getCurrentAdmin().catch(()=>null))redirect('/admin/login');const{id}=await params;if(!/^[0-9a-f-]{36}$/i.test(id))notFound();let item;try{item=await getAdminGalleryById(id)}catch{return <main className="p-8"><h1 className="text-page-title">Gallery item unavailable</h1></main>}if(!item)notFound();return <main className="px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><AdminPageHeader eyebrow="Gallery" title={`Edit ${item.title}`}/><GalleryItemForm id={item.id} initialValues={{title:item.title,description:item.description??'',mediaType:item.media_type,mediaUrl:item.media_url,thumbnailUrl:item.thumbnail_url??'',altText:item.alt_text??'',eventDate:item.event_date??''}}/></div></main>}
