import type{Metadata}from'next';import{redirect}from'next/navigation';import{AdminPageHeader}from'@/components/admin/admin-page-header';import{SettingsForm}from'@/components/admin/settings-form';import{getCurrentAdmin}from'@/lib/auth/admin';import{getAdminSettings}from'@/lib/data/admin/settings';export const metadata:Metadata={title:'Settings'};export default async function Page(){if(!await getCurrentAdmin().catch(()=>null))redirect('/admin/login');let values;try{values=await getAdminSettings()}catch{return <main className="p-8"><h1 className="text-page-title">Settings unavailable</h1></main>}return <main className="px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><AdminPageHeader eyebrow="Public Configuration" title="Site Settings" description="Manage approved information that is safe to display publicly."/><SettingsForm initialValues={values}/></div></main>}
