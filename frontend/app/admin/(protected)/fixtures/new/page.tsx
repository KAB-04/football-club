import type { Metadata } from 'next'; import { redirect } from 'next/navigation'; import { AdminPageHeader } from '@/components/admin/admin-page-header'; import { FixtureForm } from '@/components/admin/fixture-form'; import { getCurrentAdmin } from '@/lib/auth/admin'; import { getAdminTeams } from '@/lib/data/admin/players'
export const metadata:Metadata={title:'Add Fixture'}
export default async function Page(){const admin=await getCurrentAdmin().catch(()=>null);if(!admin)redirect('/admin/login');const teams=await getAdminTeams();return <main className="px-4 py-8 sm:px-6 lg:px-8 lg:py-10"><div className="mx-auto max-w-4xl"><AdminPageHeader eyebrow="Fixtures & Results" title="Add Fixture" description="Schedule a match or enter a completed historical result."/><FixtureForm id={null} teams={teams} initialValues={{teamId:'',opponent:'',competition:'',matchDate:'',venue:'',homeOrAway:'home',status:'scheduled',standfastScore:'',opponentScore:''}}/></div></main>}
